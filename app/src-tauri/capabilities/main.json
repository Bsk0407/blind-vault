{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the bundled Blind Vault window may hide itself.",
  "local": true,
  "windows": ["main"],
  "platforms": ["macOS", "windows"],
  "permissions": ["core:window:allow-hide"]
}

